import { defineConfig } from 'astro/config';

// Deployed as a GitHub Pages project site at:
// https://marcusianlevine.github.io/birds-bugs-botanicals/
export default defineConfig({
  site: 'https://bbb.marcuslevine.com',
});
